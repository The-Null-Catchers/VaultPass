import {test} from "node:test";import assert from "node:assert/strict";import {createAccount,encryptJSON,context} from "./crypto";import {restoreBackup,validateItem} from "./backup";
test("encrypted backup restores locally and rejects wrong master",async()=>{const a=await createAccount("PUBLIC FAKE BACKUP PASSWORD"),id=crypto.randomUUID(),item=validateItem({title:"Fake",type:"login",password:"NOT-A-REAL-PASSWORD"});const backup={format:"vaultpass-backup-v1",user_id:a.id,bundle:a.bundle,vaults:[{id:a.vaultId,wrapped_key:a.wrappedVaultKey}],items:[{id,version:1,payload:await encryptJSON(a.vaultKey,item,context("item",a.vaultId,id,1))}]};assert.deepEqual(await restoreBackup(backup,"PUBLIC FAKE BACKUP PASSWORD"),[item]);await assert.rejects(restoreBackup(backup,"wrong password"));assert.throws(()=>validateItem({title:"bad",type:"login",password:{unexpected:true}}));});
